import { EconomicSimulationClient } from "@/components/economic-simulation-client"

// Server Component
export async function EconomicSimulation() {
  // In a real app, fetch simulation configurations from API
  // const configurations = await fetchSimulationConfigs()
  
  return <EconomicSimulationClient />
}